opencv-python>=4.8.0
numpy>=1.24.0
scikit-learn>=1.3.0
Pillow>=10.0.0
svgwrite>=1.4.3
scipy>=1.11.0
tqdm>=4.65.0
matplotlib>=3.7.0
scikit-image>=0.21.0
# SVG->PNG backends (Stage 2) — install at least one:
#   pip install cairosvg>=2.7.0    (best quality, requires Cairo native library)
#   pip install svglib reportlab   (pure Python fallback)
#   Inkscape on PATH               (system fallback)
#   ImageMagick on PATH            (system fallback)
# JPG->SVG backend (Stage 1):
#   pip install vtracer>=0.6.0     (recommended, Rust-based, fast color tracing)
